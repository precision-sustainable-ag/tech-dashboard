import styled from 'styled-components';

export const SkeletonWrapper = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  margin: -10px -10px;

  div {
    margin: 10px 10px;
  }
`;
